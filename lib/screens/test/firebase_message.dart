import 'package:flutter/material.dart';

class TestFirebaseMessageScreen extends StatelessWidget {
  const TestFirebaseMessageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('파이어베이스 메시지')),
    );
  }
}
